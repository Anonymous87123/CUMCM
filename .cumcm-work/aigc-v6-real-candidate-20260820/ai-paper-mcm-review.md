# AI_paper MCM 工作台只读复核

候选：`humanize-run/rendered_review/main.tex`  
候选 SHA-256：`5d5fe72a74a03a92a5a669333affc9cafe1447151440f2bcf2e3ec6b91aa8a6b`  
场景：`mcm`  
边界：只读复核；未启动 GUI、远程生成或第二个语言改写器。

## 1. abstract-focus-enhancer

- 目的：摘要首段提出“资源恢复能否稳定转化为生态系统健康改善”。
- 方法：问题一至五分别给出食物网、珍稀物种响应、Hastings--Powell 链、李雅普诺夫指数和组合评价。
- 结果：1.805、1.838、0.204、3.2186、75 组扩展测试、0.662/0.423/0.155 均有数值锚点。
- 结论：末段把结论限定为禁渔收益仍受水质、连通、放流和入侵影响。
- 复核意见：结构完整。五问逐段平行使摘要略显清单化，但当前每段承担不同结果接口；不为打散结构而删数字。

## 2. argument-structure-checker

- 问题一在 1.8 倍观测、单参数标定和局地 CPUE 对照之间建立了明确链条。
- 问题二把通道损失与放流项分别落入方程，再用三组对照解释对象差异，链条闭合。
- 问题四先给短窗正指数，再用 75 组扩展测试收窄为方向性结论，未把 3.2186 写成唯一阈值。
- 待队员确认：第 207 行“呈正相关”容易被理解为统计相关，正文实际证据是同向时间变化；若代码没有相关系数或回归，宜改为“随……同步上升”。
- 待来源复核：第 274 行把附件中的 1.3 倍迁移距离用于 `gamma_B=1.3` 的参照，应在证据台账中保留附件页/表位置。

## 3. citation-format-validator

- `audit_manuscript.py` 已解析 `ref.bib`，8 个正文引用键全部存在。
- XeLaTeX--Biber--XeLaTeX 两遍后没有未定义 citation/reference。
- 文献后端为 `biber`，样式为 `gb7714-2015`；未执行格式转换，避免改动已收敛的引用键。

## 4. data-table-generator

- 正文识别到 12 个 table 浮动体、23 个 tabular 类块；未发现重复正文或空白凑页。
- 表格均使用已有结果，不生成新数值。问题二结果表、问题四稳健性表和问题五权重表后均有正文解释。
- 编译日志的 Underfull 主要位于宽表，不是数据缺失；若最终页面视觉拥挤，优先调整列宽或拆表，不改结果。

## 5. diagram-template-library

- 稿件已有 5 幅题目专属结果图，均对应一个分问；本轮无需另造研究流程图。
- 未调用通用模板，因为新图必须来自已批准的模型/代码流程，而不是为了“显得完整”增加装饰性图。

## 6. diagram-validator

- 当前图为 PNG，不是 Mermaid/JSON 图；该单元无可执行的 JSON 校验对象，记录为 `not_applicable`。
- 图文件均在隔离编译树中存在并成功载入。

## 7. flowchart-expert

- 当前没有待生成或编辑的流程图，因此不注入符号和布局建议。
- 若比赛稿后续增加算法流程图，节点只允许来自代码中的实际状态、循环和终止条件。

## 8. outline-optimizer

- 主干为问题重述、问题分析、假设、符号、五问建模求解、建议、结论、评价、参考文献和附录，分问顺序完整。
- 内容密度审计自动定位 Q1--Q5，页数依次为 3、4、1、2、2 页。
- 软提示显示显式求解段和验证段偏薄；这不是要求机械增设小标题。若队员能提供实际运行步骤、误差或复算，应写回对应分问；否则保留现状，不用算法百科补篇幅。

## 9. peer-review-simulator

- 主编视角：摘要和结论互相对应，正文含五问接口；总 PDF 52 页中约 20 页起进入附录，不能把 52 页当作正文长度。
- 方法视角：问题四的短窗阈值已被扩展测试限制；问题一“正相关”措辞仍需按实际统计量裁决。
- 领域视角：单区归一化模型多次声明不对应全流域绝对生物量，边界清楚。
- 跨学科视角：状态量、生态对象和治理建议之间基本能互相追溯。
- 反方视角：组合评价的 100% 排序稳定仅说明当前扰动协议，不代表情景设置完整；正文已把结论限制在当前比较框架。

## 10. reference-formatter

- 参考文献已由 Biber/GB/T 7714 样式生成，未检测到缺键或重复转换需求。
- 不对 `.bbl` 做手工格式化；提交时保留 `ref.bib`、`main.bcf` 和编译命令记录。

## 工作台结论

10 个 MCM 场景单元均已得到明确处置：7 个形成实质只读复核，3 个图示单元因无对应输入而记录为 `not_applicable`。没有单元产生第二份候选，也没有把研究专用的 `academic-tone-polisher`、literature-searcher 或方法设计器接到 Humanize 之后。
