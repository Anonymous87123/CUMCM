# 2024 A163 人工证据卡

- 范围：A 题，44 页；p.1--44 已逐页通读。
- 正文/评价 p.1--28，参考文献和 Python 支撑材料 p.29--44；关键栅格 p.1、4、12、15、16、24、28、37 已核。
- 证据编号：`EV-20260731-A163-01`、`EV-20260731-A163-STYLE-01`。

## 模型链

`等距螺线/弧长 -> 相邻把手位置与关联速度 -> 运动继承缩小碰撞对象 -> 四角点距离判定 -> 首次事件求精 -> 内/边界/外螺距分类 -> 两圆弧总长不变量 -> 四路径与判断函数 -> 七种跨段几何 -> 全局图缩域 -> 目标把手细搜 -> 速度比例换算`

## 可迁移写作动作

1. 速度递推先写刚性构件两端沿构件方向分速度相等，再落公式。
2. 碰撞缩域先写同轨迹继承，说明后续对象为何不会更早触发。
3. 边界搜索按“求终止状态--判内/边界/外--定方向--逐级缩步”写。
4. 先证明目标量是否仍有自由度；本篇先证总弧长与半径比无关，再采用 2:1。
5. 先命名四段路径和三个关键点，再以判断函数承接七种跨段组合。
6. 速度上限按“全时段图--峰值时段--候选把手--目标把手--细搜--比例反求”逐层缩域。
7. 沿用前问时列明切向、状态和路径段等改变项；比例换算写清成立位置。

## 边界

- 论文报告首次碰撞 `412.473894 s`、最小螺距 `0.450338 m` 和最大龙头速度 `1.246266 m/s`。
- Python 未运行，数值未独立复算；分级缩步不是灵敏度分析。
- 无独立改进方案；两圆弧不变量不外推到其他曲线族。
