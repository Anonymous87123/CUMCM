# 2022 B030: 无人机纯方位无源定位

## Evidence scope

- B 题，42 页；正文、评价、参考文献和 Python 附录均已人工逐页阅读。
- Evidence IDs: `EV-20260730-B030-01`, `EV-20260730-B030-STYLE-01`.
- Eight key raster pages were checked: p.6, p.11, p.16, p.18, p.20, p.21, p.23, p.24.

## Model chain

`polar coordinates -> three classes / nine relative-position shapes -> rotational-symmetry reduction to six sine-law models -> angular-error grid traversal -> unknown emitter ID from angular spacing -> 10,500 randomized simulations -> FY02/FY05/FY08 reference triangle on a 98 m circle -> angle correction -> radius correction -> conical-to-concentric-circle transformation around FY05 -> reuse of the circular-formation model -> special handling of FY11/FY15`

Each transition removes a named uncertainty. Classification covers the possible relative orders; symmetry removes duplicate derivations; emitter identification precedes coordinate inversion; the reference triangle separates angular and radial correction; structural transformation creates a valid interface for reusing the earlier model.

## Reported results

- Both receiving-angle errors are traversed over `[-1°,1°]` with a `0.1°` step.
- Across 10,500 randomized samples, the paper reports `100%` emitter-ID accuracy, mean angular bias `0.70`, and mean radial bias `0.54`.
- The adjustment table reports a maximum angular correction of `0.30°` for FY01 and a maximum radial correction of `14.32 m` for FY06; final non-reference radii lie in `97.68--98.56 m`.
- The conical formation is transformed into three circles centered at FY05 with radii `50 m`, `50 sqrt(3) m`, and `100 m`; FY11 and FY15 use separate geometric relations.
- `B_1.py`, `B_2.py`, and `B_3.py` implement the six localization branches, ID/error simulation, and the three adjustment stages.

## Human style rules extracted

1. Establish a complete classification, then write the observed symmetry that authorizes branch reduction.
2. Describe computational work as concrete actions: `固定……`, `遍历……`, `记录所得全部结果，并绘图输出`.
3. Authorize reuse before invoking an earlier model: `由问题一（1）中的定位模型4可知……` or `联合式（·）--（·）得……`.
4. When results are numerous, state where the full evidence is stored and which representative case the main text analyzes.
5. Explain results in the order `readout -> mechanism for the scale difference -> bounded concession -> overall comparison`.
6. For transformed problems, name the transformation and groups before saying the earlier derivation is identical and need not be repeated.

## Boundaries

The reported `100%` is a paper result, not an independent reproduction. The error grid supports only bounded perturbation evidence, not universal robustness. Long formulas are retained at the semantic level unless their raster fidelity has been checked. These observations support a single-paper writing pattern, not a frequency claim for all B papers.
