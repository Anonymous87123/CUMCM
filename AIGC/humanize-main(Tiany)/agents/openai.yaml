interface:
  display_name: "Tiany Candidate Lab"
  short_description: "Compare protected same-source writing candidates"
  default_prompt: "Use $humanize-tiany-candidate-lab to compare independent candidates against one frozen source without changing protected content."
policy:
  allow_implicit_invocation: false

