interface:
  display_name: "Humanizer"
  short_description: "Remove AI writing patterns from prose"
  default_prompt: "Use $humanizer for one pass on general prose while preserving facts; route academic, TeX, technical-result, and CUMCM files through $aigc-writing-router first."
policy:
  allow_implicit_invocation: false
