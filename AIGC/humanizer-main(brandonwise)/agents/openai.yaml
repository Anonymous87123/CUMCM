interface:
  display_name: "Brandonwise Humanizer"
  short_description: "Diagnose patterns in general English prose"
  default_prompt: "Use $humanizer-brandonwise to diagnose one general-English passage and return a source-bound candidate only when requested."
policy:
  allow_implicit_invocation: false
