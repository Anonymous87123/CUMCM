interface:
  display_name: "Patina"
  short_description: "Audit multilingual prose with semantic fidelity checks"
  default_prompt: "Use $patina to audit a prose-only proxy for style signals and semantic fidelity without treating its score as authorship evidence."
policy:
  allow_implicit_invocation: false
